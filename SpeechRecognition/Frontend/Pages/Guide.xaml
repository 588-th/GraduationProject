<Page x:Class="Interface.Pages.Guide"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Interface.Pages"
      xmlns:controls="clr-namespace:Interface.Controls" 
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Guide">

    <Grid Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border CornerRadius="5" Background="{DynamicResource BackgroundQuaternary}" Margin="10,10,10,0" Height="150" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Models" FontSize="22" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Border BorderBrush="{DynamicResource BackgroundTertiary}" HorizontalAlignment="Center" Width="150" BorderThickness="1" Margin="15,50,15,98"/>
                <TextBlock Text="Create a new model or select a model from existing ones." FontSize="16" Margin="15,60,15,30" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                <TextBlock Text="1" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <Border CornerRadius="5" Background="{DynamicResource BackgroundQuaternary}" Margin="10,10,10,0" Grid.Column="1" Height="150" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Settings" FontSize="22" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Border BorderBrush="{DynamicResource BackgroundTertiary}" HorizontalAlignment="Center" Width="150" BorderThickness="1" Margin="15,50,15,98"/>
                <TextBlock Text="Set model settings that affect the audio processing logic and model training methods." FontSize="16" Margin="15,60,15,30" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                <TextBlock Text="2" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <Border CornerRadius="5" Background="{DynamicResource BackgroundQuaternary}" Margin="10,10,10,0" Grid.Column="2" Height="150" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Dictionaries" FontSize="22" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Border BorderBrush="{DynamicResource BackgroundTertiary}" HorizontalAlignment="Center" Width="150" BorderThickness="1" Margin="15,50,15,98"/>
                <TextBlock Text="Define a dictionary of phonemes and a dictionary of words that the model will recognize." FontSize="16" Margin="15,60,15,30" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                <TextBlock Text="3" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <Border CornerRadius="5" Background="{DynamicResource BackgroundQuaternary}" Margin="10,180,10,0" Height="150" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Repository" FontSize="22" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Border BorderBrush="{DynamicResource BackgroundTertiary}" HorizontalAlignment="Center" Width="150" BorderThickness="1" Margin="15,50,15,98"/>
                <TextBlock Text="Prepare audio data for training and testing the model." FontSize="16" Margin="15,60,15,30" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                <TextBlock Text="4" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <Border CornerRadius="5" Background="{DynamicResource BackgroundQuaternary}" Margin="10,180,10,0" Grid.Column="1" Height="150" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Train" FontSize="22" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Border BorderBrush="{DynamicResource BackgroundTertiary}" HorizontalAlignment="Center" Width="150" BorderThickness="1" Margin="15,50,15,98"/>
                <TextBlock Text="Start the process of training the model and test it on test audio data." FontSize="16" Margin="15,60,15,30" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                <TextBlock Text="5" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <Border CornerRadius="5" Background="{DynamicResource BackgroundQuaternary}" Margin="10,180,10,0" Grid.Column="2" Height="150" VerticalAlignment="Top">
            <Grid>
                <TextBlock Text="Realtime" FontSize="22" Foreground="{DynamicResource TextPrimary}" FontWeight="Bold" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Border BorderBrush="{DynamicResource BackgroundTertiary}" HorizontalAlignment="Center" Width="150" BorderThickness="1" Margin="15,50,15,98"/>
                <TextBlock Text="Test the model by pronouncing words in real time." FontSize="16" Margin="15,60,15,30" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap"/>
                <TextBlock Text="6" FontSize="12" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,10" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

    </Grid>
</Page>
